# betbot-web — Variáveis de Ambiente

# URL da betbotAPI hospedada. Obrigatório em produção (Vercel).
# Exemplo: https://betbot-api.onrender.com
# Em dev local, deixar vazio (usa proxy do next.config.ts → localhost:8080).
NEXT_PUBLIC_API_BASE_URL=

# (Opcional) Habilita logs verbose do Axios no browser
# NEXT_PUBLIC_DEBUG_API=true
